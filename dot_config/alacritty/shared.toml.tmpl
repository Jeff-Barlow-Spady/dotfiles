{{- if and (eq .chezmoi.os "linux") (or (eq .chezmoi.osRelease.id "raspbian") (eq .chezmoi.osRelease.idLike "raspbian") (match "^pi" .chezmoi.hostname) (match "raspberry" .chezmoi.hostname)) }}
[env]
TERM = "xterm-256color"

[shell]
program = "zellij"

[window]
padding.x = 16
padding.y = 14
decorations = "None"
opacity = 0.98

[keyboard]
bindings = [
{ key = "F11", action = "ToggleFullscreen" }
]
{{- end }}

