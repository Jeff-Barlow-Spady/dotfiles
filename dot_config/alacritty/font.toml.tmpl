{{- if and (eq .chezmoi.os "linux") (or (and (eq .chezmoi.osRelease.id "debian") (or (match "^pi" .chezmoi.hostname) (match "raspberry" .chezmoi.hostname))) (match "^pi" .chezmoi.hostname) (match "raspberry" .chezmoi.hostname)) }}
[font]
normal = { family = "{{ .chezmoi.current_font | default "Agave Nerd Font Mono" }}", style = "Regular" }
bold = { family = "{{ .chezmoi.current_font | default "Agave Nerd Font Mono" }}", style = "Bold" }
{{- end }}

